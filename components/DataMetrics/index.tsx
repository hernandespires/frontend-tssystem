const DataMetrics = ({ department }: { department: string }) => (
    <section className="border border-default-border-color w-full"><h1 className="section-title">{ department }</h1></section>
)

export default DataMetrics